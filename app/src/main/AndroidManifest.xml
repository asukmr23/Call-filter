<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to look up whether an incoming number is a saved contact -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Needed so the call still shows correctly in the system call log -->
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- Needed to post the custom "missed call" style notification (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Needed to read basic phone state (part of the call-screening role) -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.CallFilter">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            This is the core piece: Android calls onScreenCall() on every incoming call
            BEFORE it rings, once this app is set as the system's Call Screening app.
        -->
        <service
            android:name=".CallScreeningServiceImpl"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

    </application>

</manifest>
